<%= form_with(model: cliente) do |form| %>
  <% if cliente.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cliente.errors.count, "error") %> prohibited this cliente from being saved:</h2>

      <ul>
        <% cliente.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="default-gap">
    <div>
      <%= form.label :nome, style: "display: block" %>
      <%= form.text_field :nome %>
    </div>

    <div>
      <%= form.label :telefone, style: "display: block" %>
      <%= form.text_field :telefone,:data=> {:mask=> '(00) 00000-0000'}%>
      <script> 
        (()=>{
          $("#cliente_telefone").mask("(00) 00000-0000")
        })()
      </script>
      
    </div>

    <div>
      <%= form.button "Atualizar", class:"w-100" %>
    </div>
  </div>
<% end %>
